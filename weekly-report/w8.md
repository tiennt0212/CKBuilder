| *name* | Tim Nguyen |
| *week_start_date* | 2026, June 29th |
| *week_end_date* | 2026, July 5th |

## Completed

### 1. CKBuilder — Feature Deploy Script & Tx Status Generalization
- **Developed Deploy Script Page (Phase 1 & 2)**: 
  - Successfully integrated the binary upload interface via `UploadZone`.
  - Built a complete transaction lifecycle powered by CCC (Common CMake Collection), covering transaction building, signing, and broadcasting.
  - Upgraded the Deploy Script feature to Phase 2: Added `hash_type` configuration (type/data1/data2) and a `Type ID` toggle switch (`SwitchRow`). Automatically computed the Type ID script using the `hashTypeId()` function.
- **Optimized Balance Control UI/UX**: Replaced the static `NoteBox` with a dynamic `CapacityInfoPanel` component to visually display the required CKB amount and wallet status (Sufficient/Insufficient chip), while disabling the submit button if the account balance is insufficient.
- **Generalized Transaction Statuses (Tx Status Generalization)**: Abstracted and generalized the transaction status handling logic (sending, committed, rejected) into a shared `TxStatusBanner` component, now used by both the Transfer and Deploy features.
- **Smart Contract & Assignments**: Completed and pushed the smart contract source code for `lesson-08-hash-lock` to serve binary building purposes.

### 2. FiberGate (Hackathon Progress)
- **Project Initialization & Monorepo Configuration**: Set up the monorepo directory structure (`pnpm-workspace.yaml`) and configured build settings for the development environment (`.claude`, `.context`, `tsconfig.json`).
- **CKB Solutions Research & Brainstorming**:
  - Conducted in-depth research into the Fiber Network architecture within the CKB ecosystem, understanding the mechanics of Fiber Nodes and their core features.
  - Participated in discussions and ideation sessions to define technical solutions targeting real-world CKB use cases for the Hackathon.
- **Core Services Development (In Progress)**: 
  - Implemented the `@fibergate/sdk` npm package as the core integration layer.
  - Built a secure Webhook delivery system featuring HMAC signing to enhance security, refine helpers, and reduce code duplication.
  - Established a background invoice poller paired with the `POST /api/cron/poll-invoices` API endpoint.
  - Packaged the runtime environment using a Docker Compose bundle (`fiber-node` + `postgres` + `fibergate`) and fixed Next.js app build errors inside Docker.

## Next week's plan

- **Finalize Hackathon Product (FiberGate)**: Continue developing and optimizing features connecting to the Fiber Node to ensure a timely Hackathon submission.
- **Optimize CKBuilder UI/UX**: Resolve formatting warnings (Prettier) and polish the user experience on the transaction preview dashboards.

## Key Learnings

### Deploy Script Lifecycle & Type ID Computation
- **Type ID Mechanism**: Gained a deeper understanding of computing the Type ID type script (`hashTypeId()`) precisely after `completeInputsByCapacity` and before `completeFeeBy`. Managing this exact timing ensures the deployed script transaction remains valid.
- **Centralized Tx Status Management**: Refactoring and generalizing statuses via `TxStatusBanner` keeps the codebase clean and highly reusable as CKBuilder scales to support more on-chain interaction features in the future.

### Fiber Network Architecture & Lightning-like Solutions on CKB
- **Fiber Nodes & Scalability**: Acquired a comprehensive overview of how payment channels or state channels operate on top of CKB's Fiber Network.
- **Off-chain Service Integration**: Gained hands-on experience in designing background workers (Invoice Poller) and secure Webhook delivery systems with HMAC signatures to safely synchronize states between Fiber Nodes and web applications.